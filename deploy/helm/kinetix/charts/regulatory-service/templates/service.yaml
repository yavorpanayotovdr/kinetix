apiVersion: v1
kind: Service
metadata:
  name: {{ include "regulatory-service.fullname" . }}
  labels:
    {{- include "regulatory-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "regulatory-service.selectorLabels" . | nindent 4 }}
