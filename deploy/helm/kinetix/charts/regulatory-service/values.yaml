replicaCount: 1

image:
  repository: kinetix/regulatory-service
  pullPolicy: IfNotPresent

service:
  type: ClusterIP
  port: 8085

env:
  PORT: "8085"

resources:
  requests:
    cpu: 250m
    memory: 512Mi
  limits:
    cpu: "1"
    memory: 1Gi

autoscaling:
  enabled: false
  minReplicas: 1
  maxReplicas: 5
  targetCPUUtilizationPercentage: 70

probes:
  path: /health
