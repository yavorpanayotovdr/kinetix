apiVersion: v1
kind: Service
metadata:
  name: {{ include "position-service.fullname" . }}
  labels:
    {{- include "position-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "position-service.selectorLabels" . | nindent 4 }}
